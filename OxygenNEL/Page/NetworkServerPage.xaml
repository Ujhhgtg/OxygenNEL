<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OxygenNEL.Page.NetworkServerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OxygenNEL"
    xmlns:netgame="using:OxygenNEL.Entities.Web.NetGame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:BoolNegationConverter x:Key="BoolNegationConverter"/>
        <local:StringToImageSourceConverter x:Key="StringToImageSourceConverter"/>
    </Page.Resources>

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="1" Text="网络服务器" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                <Button Content="指定服务器" Click="SpecifyServerButton_Click"/>
                <TextBox x:Name="SearchBox"
                         PlaceholderText="搜索服务器"
                         Width="200"
                         TextChanged="SearchBox_TextChanged"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="未登录"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="{Binding NotLogin, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <ScrollViewer Grid.Row="1"
                      Visibility="{Binding NotLogin, Converter={StaticResource BoolNegationConverter}}">
            <ItemsRepeater x:Name="ServersRepeater" ItemsSource="{x:Bind Servers}">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinItemWidth="260" MinItemHeight="230" MinColumnSpacing="12" MinRowSpacing="12"/>
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="netgame:ServerItem">
                        <Grid Padding="12" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Image Grid.Row="0" Height="100" Stretch="UniformToFill" Margin="0,0,0,8"
                                   Source="{x:Bind ImageUrl, Mode=OneWay, Converter={StaticResource StringToImageSourceConverter}}"/>
                            <TextBlock Grid.Row="1" Text="{x:Bind Name}" Style="{StaticResource SubtitleTextBlockStyle}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Grid.Row="2" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis">
                                <Run Text="在线人数: "/><Run Text="{x:Bind OnlineCount, Mode=OneWay}"/>
                            </TextBlock>
                            <TextBlock Grid.Row="3" Text="{x:Bind EntityId}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                            <Button Grid.Row="4" Content="加入服务器" HorizontalAlignment="Left" Click="JoinServerButton_Click" Tag="{x:Bind}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>

        <Grid Grid.Row="2" Visibility="{Binding NotLogin, Converter={StaticResource BoolNegationConverter}}" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                <Button x:Name="PrevPageButton" Content="上一页" Click="PrevPageButton_Click"/>
                <Button x:Name="NextPageButton" Content="下一页" Click="NextPageButton_Click"/>
            </StackPanel>
            <TextBlock x:Name="PageInfoText" Grid.Column="2" Margin="12,0,0,0" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>
